## 2026-07-25 - [Context-Aware Tooltips and Accessible Buttons]
**Learning:** Explaining exactly why primary buttons are disabled (e.g. empty prompt, loading repositories, quota limit exceeded) through dynamic, state-aware `title` and `aria-label` attributes prevents user confusion and makes the UI fully compliant with screen-reader accessibility expectations. Additionally, reusable wrapper elements (like `Btn`) must always forward custom properties (`...props`) to the underlying HTML element so utility attributes (like `title` or `aria-label`) can be added cleanly at implementation sites.
**Action:** When creating any button wrappers or custom interactive elements, ensure they spread `...props` onto the native button/element. Always add context-aware tooltips and ARIA descriptions to state-dependent inputs and buttons.

## 2026-07-26 - [Interactive Password Visibility and Input Accessibility]
**Learning:** For secure credential entries (like API Keys), providing a visual and keyboard-accessible "Show/Hide Password" toggle button significantly improves usability and reduces connection errors caused by transcription or copy-paste mistakes. Furthermore, associating a precise `aria-label` with the credentials input ensures that screen readers can convey the input's exact purpose without relying on adjacent textual visual elements.
**Action:** When designing credential input fields, always provide an interactive toggle to let users verify their inputs, ensure the text does not visually overlap the toggle icon by setting adequate right padding, and declare explicit ARIA label attributes for screen-reader clarity.

## 2026-07-27 - [Universal Keyboard Focus Indicators and CSS Specificity Overrides]
**Learning:** Standardizing highly accessible `:focus-visible` styles ensures that keyboard navigators have immediate, high-contrast visual feedback on their focus target without causing aesthetic clutter for mouse/touch users. When a codebase utilizes inline React/HTML styles featuring `outline: "none"`, standard CSS rules will be overridden by inline styles. Declaring `:focus-visible` with `!important` overrides inline style specificity cleanly, ensuring absolute keyboard accessibility compliance (WCAG 2.4.7 Focus Visible) across a large existing frontend without refactoring dozens of nested components.
**Action:** To ensure robust keyboard navigation support, declare global `:focus-visible` CSS rules on interactive element tags (`button`, `input`, `textarea`, `select`, `a`) and ARIA interactive roles (`[role="button"]`, `[tabindex="0"]`) using `!important` to safely override legacy inline outline resets.

## 2026-07-28 - [Descriptive ARIA Labels for Icon-Only Interactive Controls]
**Learning:** In highly compact, mobile-responsive layouts, interactive buttons are frequently rendered as standalone icons (e.g. trash cans, plus signs, chevrons, or close "x" marks) with only visual tooltips via `title` attributes. While tooltips provide mouse-hover context, assistive technologies like screen readers rely heavily on dedicated `aria-label` declarations. Without standardizing descriptive labels, these icon-only buttons remain entirely unlabelled in screen-reader navigation hierarchies, presenting a critical WCAG accessibility gap.
**Action:** Always pair `title` tooltips on interactive icon-only elements with an equivalent or superior `aria-label` matching the visual intent of the control, ensuring identical semantic context for keyboard/assistive navigators.

## 2026-07-29 - [Seamless Global Search Keyboard Shortcuts and Multi-tiered Escape Handling]
**Learning:** Integrating page-level hotkeys (like forward slash `/` to focus search) drastically elevates the power-user desktop feel, but must be paired with safety guards checking `document.activeElement` to prevent capturing inputs while typing in other forms. Additionally, implementing contextual Escape keydown handling (where first Escape clears query content, and the second collapses the panel) provides intuitive keyboard navigation. Finally, implementing Escape listeners on search dropdown fields (like repository and branch search pickers) mirrors the mouse backdrop-click behavior cleanly, creating a unified and delightful multi-tiered escape workflow.
**Action:** When creating searchable list views or dropdown pickers, always map global slash `/` shortcuts safely and bind context-sensitive `Escape` handlers to let keyboard users navigate, clear, and collapse panels seamlessly.

## 2026-07-30 - [Lifting Credentials State in Tabbed Settings & Broken Icon Prevention]
**Learning:** Moving configuration values (like API keys) from direct LocalStorage mutations within subcomponents to parent React state hooks ensures that changes instantly synchronize across background loops and network modules. Additionally, isolating primary setup parameters (like keys) into their own dedicated, discoverable tab (matching external providers' `Settings -> API` hierarchies) prevents visual clutter on general configuration pages. Finally, validating visual assets (e.g. replacing broken `'eye-off'` with mapped `'eye_closed'` keys) protects high-traffic credential fields from blank or corrupted icons.
**Action:** Always lift critical credential states to parent boundaries to keep network adapters synchronized, organize keys into dedicated tabs for maximum discoverability, and verify that icons mapped on password/key toggle inputs reference existing visual paths.

## 2026-07-31 - [Explicit ARIA Labeling on Character and Icon-Only Close Controls]
**Learning:** Visual-only symbol characters (like '×') or standalone icon tags (like `<Ic n="x" />`) used for closing modal sheets or dismissing persistent setup banners are completely opaque to screen readers or announced inappropriately (e.g., as "multiplication sign"). Providing explicit and distinct `aria-label` and `title` attributes (e.g., "Dismiss banner" and "Close Preview") is crucial to ensure identical semantic clarity and accessibility compliance across assistive device hierarchies.
**Action:** When defining dismissal triggers, overlay close banners, or custom media popup controllers, always attach highly precise 'title' and 'aria-label' attributes directly on the outer clickable elements to satisfy WCAG 2.1 accessibility criteria.

## 2026-08-01 - [Color Theme Pickers Accessibility and Character-Count Feedback Loops]
**Learning:** When developing color pickers or custom preset selectors consisting of decorative elements (like colored circular buttons), keyboard/screen-reader navigators encounter entirely opaque controls with no structural descriptions. Associating descriptive color names (e.g. "Lavender") with tooltips and ARIA attributes (like `aria-label` and `aria-pressed`) bridges the accessibility gap for visual controls. Furthermore, displaying real-time character-count feed loops for modal text entries ensures the user receives intuitive limits feedback before hitting hard `maxLength` validation boundaries.
**Action:** When implementing decorative choices or color swatch controls, always provide friendly label maps for color tags, bind context-aware `aria-pressed` states, and present subtle inline input character-count indicators alongside the input labels.

## 2026-08-02 - [Actionable Empty States and Adaptive Contextual CTAs]
**Learning:** Empty states represent a major UX touchpoint. Replacing generic "No data" textual fallbacks with illustrative, color-coded icon circles, highly contextual descriptions, and actionable Call-to-Action (CTA) buttons provides clear user pathing. When designing empty states in highly compact layout columns that can host tall temporary banners (e.g., error blocks, PWA notices), empty state paddings, icon dimensions, and typography must be meticulously downsized (e.g. 18px icons, compact paddings) to fit into dynamic viewports without triggering vertical scrollbar overflows.
**Action:** When designing feature empty lists, always construct adaptive empty states featuring contextual icon highlights, user-action description copies, and prominent CTA triggers that let users instantly resolve the empty state (such as clearing search queries or switching status tabs).

## 2026-08-03 - [Robust PR Status Recognition and Client-Side Cache Expiry]
**Learning:** Determining Pull Request status solely based on the presence of ahead commits is highly error-prone, as it can report false positives (claiming a PR is merged before it exists, or open when it has been merged). Extracting a dedicated merge-evidence analyzer that dynamically scans activity descriptions and titles for explicit keywords (e.g., "merged", "pull request merged") resolves state ambiguity. Additionally, pairing this analysis with client-side Cache Time-to-Live (TTL) expiration guarantees that users get real-time PR state updates without triggering repetitive network overhead.
**Action:** When checking remote PR metadata on the client, always verify that the PR has been created via ahead-commits count before checking its merge state, use dynamic activity description checks for explicit merge proof, and implement a 5-minute cache TTL boundary on PR state records.

## 2026-08-04 - [Persistent Draft Indicator Badges and Context-Aware ARIA Descriptions]
**Learning:** Users who save template layouts or draft messages frequently lose track of their pending works because the general dashboard offers no visual prompts of their existence, requiring them to manually click through sub-menus to audit state. Implementing highly subtle, non-intrusive status badges (like a relative circular colored dot) directly on the primary navigation triggers (like the "Drafts Box" button) instantly bridges the visibility gap. Furthermore, dynamically modifying the interactive element's ARIA label and description states ensures that assistive technologies seamlessly convey this hidden application context to non-visual navigators.
**Action:** When designing global navigation triggers or secondary utility drawers, always incorporate micro-visual badges to announce pending or cached states, and synchronize these visual markers with matching dynamic ARIA updates for robust WCAG accessibility compliance.

## 2026-08-05 - [Highly Accessible Adaptive Empty States and Tab Layout Stability]
**Learning:** Empty states are major UX touchpoint opportunities. Replacing generic "No data" or "No sessions with this status" plain text with illustrative, color-coded icon circles, highly contextual descriptions, and context-aware CTA buttons provides clear user pathing. Furthermore, keeping selected filter tab buttons rendered even when their item count dynamically drops to zero prevents layout flashing and allows the user to view the empty state of that specific tab stably.
**Action:** When designing empty state layouts, construct adaptive cards with distinct icon visual indicators and CTA actions. Maintain the rendering of the active filter tab button when empty to ensure tab layout and focus stability.

## 2026-08-06 - [Modal Keyboard Dismissal and Explicit Close Triggers]
**Learning:** Custom modals must always implement keyboard accessibility (e.g. key listeners for the `Escape` key) to prevent trapping keyboard/assistive navigators inside modal dialog states. Furthermore, pairing standard backdrop dismissal behaviors with an explicit, highly accessible visual close trigger (featuring hover states and precise `aria-label` tags) ensures maximum usability and compliance with standard screen-reader and keyboard layout expectations (WCAG 2.1).
**Action:** When creating any modal or overlay components, always bind global key event listeners for standard dismissals like `Escape` and render an explicit focusable close button equipped with clear `title` and `aria-label` attributes.

## 2026-08-09 - [Accessible Roles Selector & Focus Toggle Buttons]
**Learning:** Decorative selector chips (like role/persona selectors in a multi-persona picker) and visual toggle buttons (like "AUTO PR" or "APPROVE PLAN" controls) act as interactive boolean switches but are often opaque or confusing to screen readers and non-visual navigators. Binding explicit `title`, `aria-label`, and `aria-pressed` attributes to these controls ensures they satisfy standard WCAG 2.1 accessibility criteria, allowing screen readers to accurately announce both the state and purpose of the action.
**Action:** When implementing custom visual button arrays or interactive switch-like toggle components, always provide explicit `title` tooltips, matching descriptive `aria-label` tags, and synchronized `aria-pressed` states.

## 2026-08-10 - [Accessibility of Placeholder-Based Input and Textarea Controls]
**Learning:** Visual-only placeholder attributes are completely insufficient for screen-reader navigation on dynamic inputs (such as message composer textareas, feedback forms, search inputs, repository pickers, and starting branch entries). Because placeholders are not programmatically linked as accessible names and disappear as the user starts typing, assistive technologies cannot reliably announce the input purpose. Adding explicit `aria-label` attributes to all icon-only or placeholder-based textareas and textboxes bridges critical WCAG 2.1 accessibility gaps.
**Action:** Always complement visual-only placeholder descriptors with matching, descriptive `aria-label` tags on textareas and text input controls that do not have associated HTML `<label>` elements.

## 2026-08-11 - [Tactile Keyboard Hotkey Indicators and Semantic ARIA Tablists]
**Learning:** Overly verbose placeholder strings describing keyboard shortcuts (e.g. "(Press Esc to close)") dilute the clean visual space of search inputs and are hard to scan. Replacing these descriptors with clean, physical keyboard-styled `<kbd>` badges absolutely positioned inside inputs creates a standard premium interaction pattern. Additionally, standard list elements or divs used to trigger views (like app tabs) require explicit HTML/ARIA structures (`role="tablist"`, `role="tab"`, and `aria-selected`) to satisfy WCAG 2.1 screen-reader compliance guidelines.
**Action:** When mapping keyboard actions on visual views, render physical, tactile `<kbd>` styling guides next to or inside interactive inputs, manage target paddings to prevent overlapping text, and decorate tab rows with complete, semantic ARIA tablist structures.

## 2026-08-12 - [Localized Stateful Copy Feedback vs Thread-Blocking Alerts]
**Learning:** Reusable text copying components and action triggers should avoid using thread-blocking, jarring native browser `alert()` pop-ups to confirm clipboard writes. Transitioning these actions to localized React state-driven feedback (such as displaying "COPIED ✓" or swapping the copy icon to a success checkmark for 2 seconds) eliminates layout disruption, keeps the focus context intact, and aligns fully with premium dark-theme interface design standards.
**Action:** When implementing any copy-to-clipboard action triggers, avoid browser alert confirmation dialogs. Use localized state indicators or success checkmark icon swaps with a 2-second timeout to provide smooth, non-disruptive copy feedback.

## 2026-08-31 - [Semantic Grouping and Pressed States for Toggle Controls]
**Learning:** Segmented view mode selectors and timeframe toggles composed of raw HTML `<button>` elements (such as log view modes or chart timeframe pickers) require programmatic aria structure to be navigable and understandable by screen readers. Wrapping button groups in `role="group"` with descriptive `aria-label`s, along with assigning dynamic `aria-pressed` states and `title` tooltips to each toggle button, ensures complete WCAG 2.1 compliance and clear interactive state feedback for keyboard and assistive device users.
**Action:** When implementing custom button groups or segment controls, wrap the buttons in a container with `role="group"` and `aria-label`, attach `aria-pressed={isActive}` to each button, and supply context-aware `aria-label` and `title` tooltips.

## 2026-09-01 - [Context-Aware Step Feedback ARIA Labels in Plan View]
**Learning:** In step-by-step review workflows (like `PlanView`), small toggle buttons like "NOTE" or "EDIT" used to expand/collapse per-step feedback inputs need clear, dynamic ARIA attributes. Providing `aria-expanded` alongside state-aware `title` and `aria-label` attributes (e.g., "Add note for Step 1", "Edit feedback for Step 1", "Close feedback for Step 1", and "Finish feedback for Step 1") ensures screen reader users understand exactly which step is being annotated and whether the input drawer is expanded.
**Action:** Always add state-aware `title`, `aria-label`, and `aria-expanded` attributes to step-level annotation toggles and inline completion buttons.

## 2026-09-02 - [Standard WAI-ARIA Modal Dialog Semantics and Header Associations]
**Learning:** Reusable overlay modals require explicit `role="dialog"` and `aria-modal="true"` attributes to inform screen reader engines that interaction is confined to the overlay scope. Furthermore, programmatically linking modal headers using `aria-labelledby` and `aria-describedby` targeting title and subtitle element IDs (`modal-title`, `modal-subtitle`) ensures assistive technologies immediately announce the modal context upon focus traversal.
**Action:** Always equip reusable Modal dialog containers with `role="dialog"`, `aria-modal="true"`, and conditional `aria-labelledby`/`aria-describedby` attributes referencing title and description element IDs.

## 2026-09-03 - [Ambient Back-and-Forth Sync Border Indicators & ARIA Feedback]
**Learning:** Providing an ambient, glowing back-and-forth scanning line along container borders during active data synchronization (like refreshing session lists or activity feeds) gives users immediate visual feedback that updates are in progress without causing layout shift or content displacement. Equipping these indicators with `role="progressbar"` and descriptive `aria-label`s ensures screen reader accessibility while creating a sleek, high-tech interface feel.
**Action:** When implementing background data syncing in header containers, overlay a 2px back-and-forth scanning line along the bottom border with `role="progressbar"` and `aria-label`.

## 2026-09-04 - [Reusable Combobox Semantics and Icon Prop Propagation]
**Learning:** Reusable picker input dropdowns (like `SearchPicker`) require explicit WAI-ARIA combobox/listbox attributes (`role="combobox"`, `aria-expanded`, `aria-autocomplete="list"`, `role="listbox"`, `role="option"`, `aria-selected`) along with keyboard `Escape` dismissal to be accessible to assistive devices and keyboard users. Furthermore, SVG icon wrappers (like `Ic`) must forward `style` and remaining `...props` to the underlying `<svg>` element so custom visual transforms (like rotation transforms) and utility attributes render properly across implementation sites.
**Action:** Always furnish reusable search/picker dropdown inputs with complete WAI-ARIA combobox roles and `Escape` keydown listeners, and ensure icon wrappers forward `style` and `...props` to the native SVG tag.
