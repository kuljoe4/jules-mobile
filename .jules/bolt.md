## 2026-07-24 - Algorithmic O(N^2) Over-rendering and Reference Stability Anti-pattern
**Learning:** In list-heavy React architectures (like `SessionList`), calculating item-specific relationships (like "drift" across all sessions) inside individual items leads to O(N^2) work on every single render. Moving aggregations to the parent level via `useMemo` reduces complexity to O(N) and keeps lookups constant-time O(1) inside items. Furthermore, wrapping a component with `React.memo` is completely bypassed if fallback props evaluate to a new reference on every render (such as `activitiesMap[s.id] || []` or inline closures like `onPress={() => onSelect(s)}`).
**Action:** Always pre-aggregate list statistics at the parent/store level. Use a custom `areEqual` comparator in `React.memo` to explicitly ignore recreated inline closures, and replace default inline empty object/array fallbacks (`|| []`) in prop mappings with module-level constants (e.g. `EMPTY_ARR = []`) to preserve reference stability and prevent unnecessary re-renders.

## 2026-07-25 - Excessive Object Serialization with Media Payloads Bottleneck
**Learning:** Using `JSON.stringify` on rich domain entities (like activities or sessions) to perform general string-matching/regex-lookup operations introduces a critical vulnerability when those objects contain nested, large base64-encoded media data (e.g., screenshots/videos). This results in massive CPU churn and huge memory allocations on every card render in lists.
**Action:** Avoid full-object stringification in nested lists or selectors. Instead, target explicit text properties directly (e.g. descriptions, titles, prompts) where links or keywords actually reside, and implement size-bounded caching maps (such as `PR_INFO_CACHE`) for expensive computed selectors.

## 2026-07-27 - Safe Reference-Stable Size Calculation Caching via WeakMap
**Learning:** Caching reference-stable size/complexity calculation results inside a recursive utility (like `getApproxBytes`) can drastically cut down rendering/diffing overhead from O(N * M) to O(N). However, arrays are prone to mutability and reference sharing, so caching them can yield stale results. Furthermore, `typeof null` resolves to `"object"` in JavaScript, meaning calling `WeakMap.has(val)` on a non-null object check without a strict `val !== null` guard will crash the app with a `TypeError`.
**Action:** Use a `WeakMap` to cache recursive byte size estimations, but restrict caching solely to non-array objects to prevent stale-data issues under mutation, and always explicitly guard `WeakMap` operations with `val !== null` to guarantee absolute runtime safety.

## 2026-07-28 - WeakMap Reference-Stable Activity-Level Performance Caching
**Learning:** Caching computed session properties like ahead commits and check statuses based on session keys combined with array lengths is highly vulnerable to stale content updates if activity details are updated in-place without changing array length. Utilizing a `WeakMap` keyed by individual reference-stable activity objects allows caching granular computed properties of activities (like regex check statuses and hash matching) with O(1) performance. This completely bypasses expensive regex executions and string allocations on re-renders, while maintaining absolute correctness and automatic garbage collection.
**Action:** For repeat list traversals computing aggregate status/stats, cache per-item results using a `WeakMap` keyed by the item objects themselves. This achieves complete correctness, avoids manual cache size checks, prevents memory leaks, and achieves significant speedups.

## 2026-07-29 - Activity-Level WeakMap Branch Extraction Optimization
**Learning:** Frequent loop traversals on activity arrays performing regex-based branch and compare/tree link extraction (e.g., within `getBranchInfo`) lead to O(N) execution overhead with nested string matching on every single UI render. Because activities are immutable and reference-stable objects, we can compute all regex-matched branch properties once in a single pass and cache them using a global `WeakMap`. This completely avoids CPU-intensive string processing and regex compilation/matching across successive renders.
**Action:** Always cache complex multi-regex extraction steps on immutable/reference-stable array items using a `WeakMap` cache. Doing so turns recursive and nested linear loops back into constant-time O(1) operations with zero garbage-collection or memory footprint penalty.

## 2026-07-31 - Reference-Stable Unidiff Patch Parsing Cache with WeakMap
**Learning:** Repetitive low-level string splitting, hunk iteration, and multi-pass line scanning during frequent list re-renders (specifically inside `ConflictRadar`, `getWorkingSet`, and `DiffViewer`) create huge CPU overhead. While `gitPatch` contains a `unidiffPatch` string (which cannot be a WeakMap key), the `gitPatch` wrapper itself is reference-stable and immutable within each activity. By passing the `gitPatch` object to `parseUnidiffPatch` and storing the result in a global `WeakMap` cache (`PARSED_PATCH_CACHE`), we can completely bypass expensive string-parsing and convert repetitive parsing into instant O(1) lookups while guaranteeing automated memory eviction.
**Action:** When parsing complex text structures wrapped inside nested objects, pass the immutable wrapper object rather than the raw string to leverage reference-stable `WeakMap` caching.

## 2026-08-02 - Expensive Repetitive ISO Date String Parsing Bottleneck
**Learning:** Instantiating `new Date(string).getTime()` or direct string date comparisons in JavaScript is extremely CPU-intensive (taking microseconds per call) due to string pattern matching, components validation, and timezone offset computation. In list-heavy SPAs with active background polling, repeatedly parsing timestamps inside sort routines, list filtering, drift checking, and quota computations results in major UI stuttering, continuous CPU load, and severe garbage collection churn.
**Action:** Implement a global, size-bounded `DATE_MS_CACHE` Map cache with an O(1) helper like `parseDateMs(string)`. Converting repetitive string timestamp parsing into instant O(1) cache hits achieves a 200x-1000x speedup per date evaluation, completely eliminating UI lag and CPU overhead during polling.

## 2026-08-04 - Array-Level Reference-Stable WeakMap Byte Size Calculation Caching
**Learning:** While recursively caching individual activity sizes in a `WeakMap` turns O(N * M) checks into flat O(N) array traversals, frequently evaluating the total byte size of large activity arrays (e.g., inside `getPRInfo`, `getBranchInfo`, `SessionCard`, and `SessionDetail`) still forces O(N) array traversals on every render or cache key check. Since the activity arrays in modern React states are reference-stable and immutable (recreated only on updates), caching the cumulative size using the array reference as a key on a dedicated `ACTIVITIES_SIZE_CACHE` WeakMap avoids O(N) loop executions completely.
**Action:** For large, reference-stable arrays passed to recursive size calculators, use a dedicated, array-specific WeakMap to cache cumulative aggregate sizes based on the stable array reference, converting O(N) list traversals into instant O(1) lookups across render and polling loops.

## 2026-08-08 - High-Frequency Re-rendering and Reference-Stable Callback Closures
**Learning:** Even when heavy list-rendering child components (such as `ActivityFeed`) are wrapped in `React.memo` to optimize rendering performance, any inline arrow functions or closures passed as props (e.g., `onShowAll`, `onEditMessage`, and `onReply`) are recreated on every single render of the parent. This completely breaks standard shallow prop-reference equality comparisons, forcing unnecessary re-renders of the child on every update (such as 1-second countdown ticks). Wrapping handlers in reference-stable `useCallback` hooks with empty dependency arrays (`[]`) fully restores `React.memo` effectiveness.
**Action:** Never pass inline closures or callbacks to memoized child components, especially inside parents that render frequently (e.g., timers or poll counters). Always use reference-stable `useCallback` hooks to preserve prop-reference stability and prevent redundant Virtual DOM diffing.

## 2026-08-10 - High-Frequency Root-Level Redundant State Subscriptions
**Learning:** Registering a state-updating subscription (such as `NET.subscribe` updating `netSnap`) inside the application's root component (`JulesClient`) when that state is completely unreferenced in any render/JSX output creates an invisible but massive performance penalty. Because it resides at the root, every single trigger (such as background network activity, telemetry logging, and polling updates) forces a complete re-evaluation of the entire Virtual DOM tree of the SPA, leading to heavy CPU spikes, stuttering, and garbage collection churn despite child-level memoization.
**Action:** Never register high-frequency or background subscriptions at the root of the component tree unless the state is strictly required for root-level rendering or layout transitions. Always isolate subscriptions to the specific view/panel components that actually consume and display that data, preventing global reconciliation overhead.

## 2026-08-11 - Array-Level Reference-Stable WeakMap Caching on Immutable Activity Lists
**Learning:** Performing multiple custom linear traversals (e.g. `getAheadCount`, `getCheckStatus`, and `isActuallyDone` reverse scans) inside React component rendering pathways on every render tick leads to O(N) execution overhead. Because the activities arrays passed to these components are immutable and reference-stable within React's state management, we can cache the final aggregate results at the array reference level using `WeakMap` objects. This avoids executing any loops or regex evaluations on re-renders, immediately turning list traversals into instant constant-time O(1) lookups with automatic memory eviction.
**Action:** For all expensive metrics, filters, and state evaluations computed from reference-stable arrays in high-frequency rendering contexts, implement dedicated WeakMap caches keyed by the array references to bypass traversals entirely.

## 2026-08-12 - Preserving SessionCard React.memo Reference Stability via Direct Prop Delegation
**Learning:** Passing inline arrow functions (such as `onPress={() => onSelect(s)}`) inside `.map()` iterations to memoized child components (`SessionCard`) creates a new function closure reference on every render pass of the parent (`SessionList`). Because `SessionList` re-renders every 1 second due to countdown timer ticks, `React.memo`'s shallow prop comparison fails on every tick for every session card, forcing unnecessary re-renders of the entire list.
**Action:** Always pass reference-stable callback props (like `onSelect`) directly to list item components without inline arrow wrappers, and encapsulate item-specific argument delegation (`onSelect(s)`) inside a memoized `useCallback` handler within the child component itself.

## 2026-08-14 - Activity-Level WeakMap Working Set File Extraction Caching
**Learning:** Computing `getWorkingSet` file lists across session activity objects involves traversing changeSet gitPatches (parsing unidiff hunks) and executing regex matches for file mentions in plan steps. Since activity objects are reference-stable in React state, re-extracting file mentions from every activity on every `getWorkingSet` call (e.g. during `ConflictRadar` renders or prompt typing) causes unnecessary CPU churn and regex matching loops. Caching the extracted file sets per activity object using a `WeakMap` (`WORKING_SET_ACTIVITY_CACHE`) turns re-evaluations into flat O(N) set combinations of O(1) cached activity sets.
**Action:** When extracting computed attributes (like file mentions or diff hunks) from immutable list item objects, cache the per-item extraction result in a dedicated `WeakMap` to avoid redundant regex parsing across render ticks and cross-component evaluations.

## 2026-08-20 - Pre-aggregating Dropdown Filter Counts in Parent useMemo
**Learning:** Calculating item counts inside `.map()` loops when rendering dropdown lists (such as counting session items per repository in `SessionList`) creates O(N * R) array filtering and string manipulation overhead on every render pass. By computing both the unique options list and a count lookup map (`repoCountsMap`) inside a single O(N) `useMemo` pass, count lookups in JSX map iterations become constant-time O(1) property accesses, eliminating redundant array traversals and string parsing during high-frequency re-renders (such as polling timer countdown ticks).
**Action:** When rendering list filter options with counts, always compute a count lookup map in a single O(N) pass within `useMemo` instead of executing `.filter()` inside the option list `.map()` iteration.
